import React from 'react';
// import {a} from 'react-router-dom';

const BackToTop = (props) => (<a href={props.destination} className="fixed-bottom text-right"><i className="fa fa-arrow-circle-up fa-2x text-success"></i></a>);

export default BackToTop;